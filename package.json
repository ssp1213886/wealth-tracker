{
  "name": "wealth-server",
  "version": "1.0.0",
  "description": "个人投资仪表盘：持仓追踪、实时行情与 Cloudflare 云端同步",
  "main": "src/worker.js",
  "scripts": {
    "check": "npm test && npx wrangler deploy --dry-run",
    "deploy": "npx wrangler deploy",
    "dev": "npx wrangler dev",
    "preview:lan": "node scripts/lan-preview.mjs",
    "test": "node --test tests/*.test.mjs",
    "lint": "node scripts/lint.mjs",
    "build": "node scripts/build.mjs"
  },
  "keywords": [
    "portfolio",
    "investment",
    "cloudflare-worker"
  ],
  "author": "ssp1213886",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.104.0"
  },
  "type": "module"
}
